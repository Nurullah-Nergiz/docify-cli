import { writeFileSync } from 'fs'
import { dirname } from 'path'

export default function writeCacheFile(cacheValue) {
   //    const cachePath = `${__dirname}/../../.cache/data.json`
   console.log('path', import.meta.url)
   // try {
   //     writeFileSync()
   // } catch (err) {

   // }
}
